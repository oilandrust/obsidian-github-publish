/** Set at build time via esbuild `define` (see esbuild.config.mjs). */
export const showAdvancedSettings = __SHOW_ADVANCED_SETTINGS__;
export const isDevBuild = __IS_DEV_BUILD__;
export const buildCommit = __BUILD_COMMIT__;
